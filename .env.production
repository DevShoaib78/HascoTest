# Production environment — values baked into the build at build time.
# Next.js loads this automatically when NODE_ENV=production (i.e. during `npm run build`).
# NEXT_PUBLIC_* values are public; safe to commit.

NEXT_PUBLIC_SITE_URL=https://hasco.com.sa
